[project]
name = "utilityhub-logging"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rajesh", email = "hyperoot.tech@proton.me" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
utilityhub-logging = "utilityhub_logging:main"

[build-system]
requires = ["uv_build>=0.11.22,<0.12.0"]
build-backend = "uv_build"
